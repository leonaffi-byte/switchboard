# Review round 1 findings (workflow wf_13b0a1f2-65a, 2026-09-05). Verifier r1: PASS 12/12. Verdicts: 3x APPROVE_WITH_FIXES, 0 blockers.

[major] (spec-correctness) /home/leoom/Projects/switchboard-wt-id/Panel.qml:986-990
  WHAT: The 'Save or replace the current login first' tooltip on held switch rows is unreachable: the Button sets enabled:false, and the shell Button's ToolTip is bound to mouseArea.containsMouse, but QtQuick (qt6-declarative 6.11.2 here) never delivers hover events into a disabled item's subtree, so containsMouse stays false.
  SCENARIO: Live login is B, marker slot 'leoleo' holds A (login_state:'unsaved' with login_conflict_label). Every saved row's switch Button gets enabled:false. The user hovers a greyed row for 2 s: no tooltip appears, and nothing on screen says why the rows are dead unless they scroll to the live-login block. The spec's 'disabled row clicks during a conflict + tooltip' requirement is only half met.
  FIX: Keep the Button enabled while held and make the click a no-op: `enabled: !claudeRow.renaming && (!claudeRow.switchable || (!!root.svc && !root.svc.busy))`, `onClicked: if (claudeRow.switchable && root.svc && !claudeRow.held) root.svc.switchEntry(claudeRow.entry)`, and dim via `opacity: claudeRow.held ? 0.5 : 1` (or set foreground: Color.muted). Alternatively wrap the row in an enabled Item carrying its own HoverHandler + ToolTip. Update the pinned regex in model.test.mjs (the `enabled:` assertion in the ClaudeAccountRow contract block) to the new expression plus a `!claudeRow.held` guard on onClicked.

[major] (spec-correctness) /home/leoom/Projects/switchboard-wt-id/Model.js:514-523
  WHAT: §9.3 state 2 ('login matches saved account Y', plain Save) is unreachable: the backend's Unsaved state always carries conflict.label (config.rs:266 LoginConflict.label is String, config.rs:553 sets it to the marker) so login_conflict_label is always present with unsaved, and loginRowState tests the conflict label before login_matches_label; the model test fixture (model.test.mjs:196) omits login_conflict_label, a shape the backend never emits.
  SCENARIO: Marker 'leoleo' holds A, slot 'work' holds B (older tokens), user /login's as B. Backend emits login_state:'unsaved', login_conflict_label:'leoleo', login_matches_label:'work'. Widget shows the red 'Logged in as b@… — not “leoleo” (a@…)' line with the field pre-filled 'work' under a button labelled 'Save as new' — the user is told to 'save as new' a name that already exists, and the spec's muted 'login matches saved account “work”' caption never appears. Clicking works (backend allows the verified overwrite) but the copy contradicts itself, and a user who edits the name instead ends up with a third slot for the same account.
  FIX: In loginRowState return "matches" when `state === "unsaved" && labelValue(entry.login_matches_label) !== ""` BEFORE the conflict check (loginConflict still holds the rows since it reads the conflict label). In Panel.qml show Model.matchesCaption as the caption for loginRow === "matches" (optionally keep the conflict line above it) and keep the Replace opener visible for that state too. Fix the fixture at model.test.mjs:196 to include login_conflict_label:'leoleo' and assert loginRowState === 'matches' and loginConflict !== null.

[major] (spec-correctness) /home/leoom/Projects/switchboard-wt-id/Model.js:36
  WHAT: BACKEND_SHA256 (and README.md lines 21, 30, 33, 182, 195) still pin v1.9.1-whkey.3 = 34a9fd4, a backend that emits none of the identity fields, while the comment at Model.js:234 says 'backend >= whkey.4'; spec acceptance criterion 11 is unmet.
  SCENARIO: The widget ships as-is: the hash gate accepts only the whkey.3 binary, which never sends login_state, so every new panel state silently degrades to the legacy 'unsaved login' Save row and the conflict hold never engages — exactly the incident the change is meant to fix. Nothing breaks visibly, so the regression goes unnoticed.
  FIX: After the backend release is cut and attested: set BACKEND_SHA256 to the sha256sum of the new ai-usagebar-x86_64-unknown-linux-gnu asset, update the tag/commit/hash in README.md (lines 21, 30, 33, 182, 195), and make the version comments at Model.js:35, 38 and 234 agree on the same tag. Treat this as a release-chain gate, not a code fix.

[minor] (spec-correctness) /home/leoom/Projects/switchboard-wt-id/Panel.qml:519-548
  WHAT: The Update confirm in the unverified state has no cancel path when 'Save as new…' has not been opened: the openers row hides while confirmShown, the field (whose edit clears replaceConfirmLabel) is hidden, so the only exits are the destructive Update click or the next parsed report (refreshIntervalSec >= 60 s).
  SCENARIO: identity_check:'unavailable', user clicks “It's “leoleo” – update” to read the warning, decides against it. The red 'If b@… is not “leoleo”, “leoleo” is lost.' + Update row stays for up to five minutes with the openers gone; the only visible button is the destructive one.
  FIX: Add a quiet `Button { text: "Cancel"; foreground: Color.muted; onClicked: root.svc.replaceConfirmLabel = "" }` to the confirm row (or make the opener a toggle), and extend the model.test.mjs confirmRow assertions to require it.

[minor] (spec-correctness) /home/leoom/Projects/switchboard-wt-id/Panel.qml:475-512
  WHAT: The unverified openers row (“It's “<label>” – update” + “Save as new…”) is an unconstrained Row of non-eliding Buttons inside a Style.space(300) panel; labels longer than roughly 20 characters push the second button past the panel edge.
  SCENARIO: Marker label 'personal-work-account-2026' (26 chars, valid). At caption size the first button is ~230 px, 'Save as new…' ~90 px, total > 300 px: 'Save as new…' is clipped or invisible, leaving only the destructive opener on screen.
  FIX: Cap the label in the opener text (e.g. `Model.quoted(root.replaceTarget, 16)` with an ellipsis, keeping the full label in the confirm caption) or lay the openers out in a `Flow { width: parent.width }` so the second button wraps instead of clipping.

[nit] (spec-correctness) /home/leoom/Projects/switchboard-wt-id/Model.js:231-240
  WHAT: plan_changed (emitted by the backend per amendment 2 as display-only) is neither whitelisted in normalizeEntry nor listed in the README protocol paragraph; it is silently ignored.
  SCENARIO: A plan upgrade re-login produces plan_changed:true on the saved row; the widget shows nothing. Harmless today, but the field exists only for display and has no consumer.
  FIX: Either add `plan_changed: raw.plan_changed === true` to normalizeEntry with a muted caption ('plan changed, saved copy updated'), or note in the README protocol paragraph that plan_changed is emitted but not rendered.

[nit] (spec-correctness) /home/leoom/Projects/switchboard-wt-id/Model.js:448-450
  WHAT: validAccountLabel requires a leading [a-z0-9] while the backend accepts [a-z0-9_-]+ (flat_accounts.rs:802), so a CLI-created marker like '_work' makes login_conflict_label sanitise to '' and the conflict degrades to the plain Save row with no row hold (the backend still refuses the switch).
  SCENARIO: account save _work via the CLI, then /login as another account. Widget shows 'unsaved login' + Save instead of the conflict block; switch rows stay enabled and a click surfaces the backend's different_login_error as a status error.
  FIX: Pre-existing asymmetry (such rows are not switchable in the widget either). Either align the widget regex with the backend's or document that labels must start with a letter or digit to be managed from the widget.

[nit] (spec-correctness) /home/leoom/Projects/switchboard-wt-id/README.md:203-220
  WHAT: The 'Credential-file safety' section dropped its line references instead of re-basing them as §9.4 asked, while the sentence 'All file references below are at the pinned revision' (line 203) was kept.
  SCENARIO: A reviewer following the README looks for line numbers that are no longer there; the retained sentence now refers to nothing.
  FIX: Either re-add the re-based line refs (switch_under_lock, resync_marker_same_account_under_lock, recover_under_lock, validate_switch, identity::verdict) at the pinned revision, or delete the 'All file references below…' sentence.

[minor] (security-concurrency) /home/leoom/Projects/switchboard-wt-id/Model.js:35-37
  WHAT: BACKEND_SHA256 still pins the whkey.3 asset (b2134dc1…) while the new parsing comment says 'backend >= whkey.4'; only the version comment was corrected. Until the release is cut and the hash bumped the widget runs against a backend that never emits login_state, so the new conflict/unverified UI is unreachable and auto-switch suppression for 'unverified' never triggers.
  SCENARIO: Merged as-is: the incident sequence on a machine with the pinned whkey.3 backend still shows the legacy 'unsaved login' row only (the backend, not the widget, is what refuses the overwrite), and a user reading the README expects the identity block that cannot appear.
  FIX: Release-checklist item (§12.11): after the attested build, update BACKEND_SHA256, the README hash line and the two version comments in the same commit; consider a model.test.mjs assertion that the comment version and the README version string agree so they cannot drift again.

[minor] (ux-contract) /home/leoom/Projects/switchboard-wt-id/Model.js:~520 (function loginRowState, the `return "conflict"` check)
  WHAT: Conflict is checked before matches, so spec §9.3 case 2 ("login matches saved account Y" + plain Save) is unreachable with the real backend: `apply_login_state` (report.rs) always sets `login_conflict_label` for `unsaved`, and the unit test only reaches "matches" with a synthetic entry that lacks the conflict label.
  SCENARIO: Live = B, marker = leoleo (A), slot leonaffi already holds B → report `{login_state:"unsaved", login_conflict_label:"leoleo", login_matches_label:"leonaffi"}` → the panel shows the red "Logged in as b@… — not “leoleo” (a@…)" line, a button labelled "Save as new" pre-filled with `leonaffi` (an EXISTING slot it will overwrite via the verified re-sync), and a "Replace “leoleo”…" opener, instead of the muted "login matches saved account “leonaffi”" caption with Save. Outcome is still the intended `account save leonaffi`, but the copy is wrong and the destructive opener is offered where the spec wants the quiet path.
  FIX: In `loginRowState`, for `state === "unsaved"` test `labelValue(entry.login_matches_label) !== ""` → "matches" BEFORE the conflict check (keep `loginConflict()` so switch rows stay held). Update the 'live-login states drive…' test so the `matches` fixture carries `login_conflict_label` like a real report.

[nit] (ux-contract) /home/leoom/Projects/switchboard-wt-id/Model.js:36
  WHAT: `BACKEND_SHA256` still pins the v1.9.1-whkey.3 asset while the new report fields are commented as "backend >= whkey.4"; §12.11 (hash equals the attested new asset, README hash updated) cannot be met until that release exists.
  SCENARIO: Shipping the widget as-is against the old pinned backend: every new field is absent, so the panel silently falls back to the legacy unsaved row and the conflict hold/Replace flow never appears; shipping the new backend without bumping the hash: the widget refuses to execute it.
  FIX: Release checklist: build and attest v1.9.1-whkey.4, then update `BACKEND_SHA256`, the two release-binding comments, and the README hash in the same commit.
